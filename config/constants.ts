// --- Animation & Transition Durations ---
export const DURATION_NORMAL = 300; // ms
export const DURATION_FAST = 200; // ms
export const DURATION_SLOW = 500; // ms

// --- Interaction Thresholds ---
export const SWIPE_THRESHOLD = 50; // pixels
export const SCROLL_THROTTLE_MS = 800; // ms for wheel-based view switching

// --- Clock ---
export const CLOCK_RETURN_ANIMATION_MS = 700; // ms for the smooth return animation
